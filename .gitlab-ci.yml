stages:
  - build

build:
  stage: build
  tags:
  - windows
  - shell
  script:
    - dotnet restore Faelyn.sln
    - dotnet build --no-restore Faelyn.sln
  artifacts:
    paths:
      - ./Build/tmp/out
    expire_in: 1 month